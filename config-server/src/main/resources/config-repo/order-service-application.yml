server:
  port: 7072
spring:
  main:
    banner-mode: off
  security:
    user:
      name: user
      password: 123456789
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: root
  datasource:
    username: admin
    password: root
    url: jdbc:postgresql://localhost:5433/orderDB
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

feign:
  customer-base-url : http://localhost:7071/api/v1/customer


